# Tasks: Theme Renaming

- [x] Implement renaming script `tools/rename-themes.py`
- [x] Normalize size and orientation strings (e.g. `5.0`, `V`, `H`)
- [x] Implement size inference from background image dimensions
- [x] Implement automatic `theme.yaml` updates for inferred sizes
- [x] Execute renaming on all themes in `res/themes/`
- [x] Handle edge cases (already renamed, missing yaml, nesting)
